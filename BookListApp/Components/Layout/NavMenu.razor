<nav class="sidebar-nav">
    <div class="nav-header">
        <div class="nav-logo">📚</div>
        <div class="nav-title">Bibliothek</div>
        <div class="nav-subtitle">Ihre Sammlung</div>
    </div>

    <div class="nav-menu">
        <NavLink class="nav-link" href="/" Match="NavLinkMatch.All">
            <span class="nav-link-icon">🏠</span>
            <span class="nav-link-text">Startseite</span>
            <span class="nav-link-badge"></span>
        </NavLink>

        <NavLink class="nav-link" href="books">
            <span class="nav-link-icon">📖</span>
            <span class="nav-link-text">Meine Bücher</span>
            <span class="nav-link-badge"></span>
        </NavLink>

        <NavLink class="nav-link" href="favorites">
            <span class="nav-link-icon">⭐</span>
            <span class="nav-link-text">Favoriten</span>
            <span class="nav-link-badge"></span>
        </NavLink>
    </div>

    <div class="nav-footer">
        <div class="user-info">
            <div class="user-avatar">👤</div>
            <div class="user-details">
                <div class="user-name">Leser</div>
                <div class="user-status">Online</div>
            </div>
        </div>
    </div>
</nav>

<style>
    .sidebar-nav {
        height: 100vh;
        display: flex;
        flex-direction: column;
        padding: 1.5rem 0;
    }

    .nav-header {
        padding: 0 1.5rem 2rem 1.5rem;
        border-bottom: 1px solid rgba(255, 255, 255, 0.1);
        margin-bottom: 1.5rem;
    }

    .nav-logo {
        font-size: 3rem;
        text-align: center;
        margin-bottom: 0.5rem;
        filter: drop-shadow(0 4px 8px rgba(0, 0, 0, 0.2));
    }

    .nav-title {
        color: white;
        font-size: 1.5rem;
        font-weight: 700;
        text-align: center;
        margin-bottom: 0.25rem;
        text-shadow: 0 2px 4px rgba(0, 0, 0, 0.3);
    }

    .nav-subtitle {
        color: rgba(255, 255, 255, 0.8);
        font-size: 0.9rem;
        text-align: center;
        font-weight: 400;
    }

    .nav-menu {
        flex: 1;
        display: flex;
        flex-direction: column;
        gap: 0.5rem;
        padding: 0 1rem;
    }

    .nav-link {
        display: flex;
        align-items: center;
        gap: 0.75rem;
        padding: 1rem 1.25rem;
        color: white;
        text-decoration: none;
        border-radius: 12px;
        transition: all 0.3s ease;
        position: relative;
        background: rgba(255, 255, 255, 0.05);
        border: 1px solid rgba(255, 255, 255, 0.1);
    }

        .nav-link:hover {
            background: rgba(255, 255, 255, 0.1);
            transform: translateX(8px);
            border-color: rgba(255, 255, 255, 0.2);
        }

        .nav-link.active {
            background: rgba(255, 255, 255, 0.15);
            box-shadow: 0 8px 25px rgba(0, 0, 0, 0.2);
            border-color: rgba(255, 255, 255, 0.3);
        }

            .nav-link.active::before {
                content: '';
                position: absolute;
                left: -1rem;
                top: 50%;
                transform: translateY(-50%);
                width: 4px;
                height: 60%;
                background: #ffd700;
                border-radius: 2px;
            }

    .nav-link-icon {
        font-size: 1.3rem;
        width: 24px;
        text-align: center;
        transition: transform 0.3s ease;
    }

    .nav-link:hover .nav-link-icon {
        transform: scale(1.2);
    }

    .nav-link-text {
        font-size: 1rem;
        font-weight: 500;
        flex: 1;
    }

    .nav-link-badge {
        width: 6px;
        height: 6px;
        background: #ffd700;
        border-radius: 50%;
        opacity: 0;
        transition: opacity 0.3s ease;
    }

    .nav-link.active .nav-link-badge {
        opacity: 1;
    }

    .nav-footer {
        padding: 1.5rem 1.5rem 0 1.5rem;
        border-top: 1px solid rgba(255, 255, 255, 0.1);
        margin-top: auto;
    }

    .user-info {
        display: flex;
        align-items: center;
        gap: 0.75rem;
        padding: 0.75rem;
        background: rgba(255, 255, 255, 0.05);
        border-radius: 10px;
        border: 1px solid rgba(255, 255, 255, 0.1);
    }

    .user-avatar {
        font-size: 1.5rem;
        background: rgba(255, 255, 255, 0.1);
        padding: 0.5rem;
        border-radius: 8px;
    }

    .user-details {
        flex: 1;
    }

    .user-name {
        color: white;
        font-weight: 600;
        font-size: 0.9rem;
    }

    .user-status {
        color: rgba(255, 255, 255, 0.7);
        font-size: 0.8rem;
    }

    @@media (max-width: 768px) {
        .sidebar-nav {
            height: auto;
            padding: 1rem 0;
        }

        .nav-menu {
            flex-direction: row;
            overflow-x: auto;
            padding: 1rem;
            gap: 0.5rem;
        }

        .nav-link {
            min-width: 120px;
            flex-direction: column;
            text-align: center;
            gap: 0.5rem;
            padding: 1rem 0.75rem;
        }

            .nav-link:hover {
                transform: translateY(-5px);
            }

            .nav-link.active::before {
                left: 50%;
                top: -0.5rem;
                transform: translateX(-50%);
                width: 60%;
                height: 3px;
            }
    }
</style>
